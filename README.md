# 3D Printing Platform - Frontend

Vue.js фронтенд для платформы 3D печати.

## Технологии

- **Vue 3** - прогрессивный JavaScript фреймворк
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик
- **Vue Router** - маршрутизация
- **Pinia** - управление состоянием
- **Tailwind CSS** - utility-first CSS фреймворк
- **Axios** - HTTP клиент

## Установка

### Требования

- Node.js 18+
- npm или yarn

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd frontend
```

2. Установите зависимости:
```bash
npm install
# или
yarn install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл с URL вашего API
```

## Запуск

### Режим разработки

```bash
npm run dev
# или
yarn dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Продакшн сборка

```bash
npm run build
# или
yarn build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр продакшн сборки

```bash
npm run preview
# или
yarn preview
```

## Структура проекта

```
frontend/
├── public/            # Статические файлы
├── src/
│   ├── api/          # API клиенты
│   ├── assets/       # Изображения, стили
│   ├── components/   # Vue компоненты
│   │   ├── admin/   # Админ панель
│   │   ├── layout/  # Компоненты макета
│   │   └── ...
│   ├── composables/  # Переиспользуемая логика
│   ├── router/       # Настройка маршрутизации
│   ├── stores/       # Pinia stores
│   ├── types/        # TypeScript типы
│   ├── views/        # Страницы приложения
│   ├── App.vue       # Корневой компонент
│   └── main.ts       # Точка входа
├── .env.example      # Пример конфигурации
└── package.json      # Зависимости
```

## Конфигурация

### Переменные окружения

Создайте `.env` файл на основе `.env.example`:

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

## Основные функции

### Для пользователей
- 📦 Каталог услуг 3D печати
- 📝 Оформление заказов
- 📁 Загрузка 3D моделей
- 🎨 Выбор цветов и материалов
- 📊 Отслеживание заказов
- 📰 Блог и новости
- 💬 Отзывы

### Для администраторов
- 👥 Управление заказами
- 🛠️ Управление услугами
- 🎨 Управление цветами
- 📝 Управление контентом (статьи, отзывы)
- 📊 Статистика и аналитика

## Разработка

### Линтинг

```bash
npm run lint
# или
yarn lint
```

### Форматирование

```bash
npm run format
# или
yarn format
```

### Проверка типов

```bash
npm run type-check
# или
yarn type-check
```

## Деплой

### Статический хостинг (Vercel, Netlify, etc.)

1. Соберите проект:
```bash
npm run build
```

2. Загрузите содержимое папки `dist/` на хостинг

### Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### Docker

```dockerfile
FROM node:18-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл с реальными данными
- Используйте HTTPS в продакшене
- Настройте правильные CORS политики на бэкенде
- Регулярно обновляйте зависимости

## Лицензия

Proprietary
